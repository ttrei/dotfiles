general {
        output_format = i3bar
        colors = true
        interval = 2
}

order += "disk /"
order += "ethernet enp5s0"
order += "cpu_usage"
order += "time"

ethernet enp5s0 {
        format_up = "E: %ip"
        format_down = "E: down"
}

time {
        format = "%Y-%m-%d %H:%M"
}

cpu_usage {
        format = "%usage"
}

disk "/" {
        format = "%free"
}
